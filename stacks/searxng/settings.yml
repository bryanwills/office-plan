use_default_settings: true

general:
  instance_name: "ai-nuc search"

server:
  limiter: false
  image_proxy: true
  method: "GET"

search:
  safe_search: 0
  autocomplete: ""
  default_lang: "en"
  formats:
    - html
    - json
