# Copy to /opt/stacks/ollama-gateway/.env on the NUC (mode 600). Never commit.
# Clients (open-interpreter, etc.) send: Authorization: Bearer <this value>
OLLAMA_PUBLIC_KEY=replace-with-openssl-rand-hex-32
